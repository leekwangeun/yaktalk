# 제2회 파이썬 SW 활용 경진대회 — 프로젝트 지침

포스터(`KakaoTalk_20260701_162057878.png`)와 제공된 양식 문서(`[참가신청서]...doc`, `[최종보고서]...doc`)를 바탕으로 정리한 대회 참가 가이드.

## 대회 개요
- 주최: 광운대학교 공학교육혁신센터
- 참가대상: 파이썬 SW 활용에 관심있는 광운대생 (**2인 이상 팀 구성 필수**)
- 상격: 공학교육혁신센터장상

## 모집 분야 (택1)
| 분야 | 대상 | 주제 |
|---|---|---|
| 1. 기초활용 (Insight, 문제기반) | 문과계열 5팀 | 스마트한 대학생활(학습, 캠퍼스 편의 등) |
| 2. 심화활용 (Innovation, 기술기반) | 이과계열 5팀 | 지역/사회 문제 해결 창의적 솔루션 (**공공데이터 활용 필수**) |

## 일정
| 단계 | 기한 |
|---|---|
| 신청서 + 제안서(자유양식) 제출 | **2026.7.17(금) 14:00까지** → atnqls3671@kw.ac.kr |
| 1차 서면심사 (분야별 5팀 이상 신청 시에만 진행) | - |
| 본선진출자 발표 | - |
| 멘토링 (온라인) | 2026.7.25(토)~26(일) |
| 결과보고서 + 분석코드(GitHub/Zip) + 발표자료(자유양식) 제출 | **2026.7.30(목)** |
| 경진대회 (본선, 발표 5-10분+질의응답 5-10분) | **2026.8.6(목) 13:00** |

※ 일정은 상황에 따라 변동 가능

## 심사 기준
문제 정의, 데이터 분석 및 전처리, AI 모델의 적합성, 창의성 및 실용성, 발표와 문서화

## 시상 내역
| 부문 | 팀수 | 부상 |
|---|---|---|
| 최우수 | 2팀 | 40만원 |
| 우수 | 2팀 | 25만원 |
| 장려 | 2팀 | 10만원 |
(분야별 1팀씩 시상)

## 제출 서류 구성

### 1. 참가신청서 (`[참가신청서]...doc`, 1차 제출용)
- 팀명 / 작품 주제 / 참가 분야
- 팀원 구성 정보: 성명, 학과, 학년, 학번, 연락처, E-mail
- 개인정보 수집·활용 동의 (팀장/팀원 서명, **서명은 이미지로 첨부**)
- 신청인(대표학생) 서명

### 2. 제안서 (자유양식, 1차 제출용 — 별도 템플릿 없음)
신청서와 함께 7/17까지 제출. 형식 자유이므로 아래 결과보고서 목차를 참고해 문제 정의·해결 방향을 간략히 작성하면 무난함.

### 3. 결과보고서 (`[최종보고서]...doc`, 최종 제출용 — 2026.7.30까지)
- 팀명 / 작품주제 / 참가 분야 / 한 줄 요약
1. 문제 정의 및 필요성 — 해결하려는 문제 정의, 선정 이유
2. 관련 사례 또는 선행 연구 조사 — 유사 서비스/사례 비교 및 차별점
3. 데이터 수집 및 전처리 — 데이터 출처, 규모, 처리 방법, 변수 설명 (심화 지원팀은 모델 설계·분석 방법 포함)
4. 실험 결과 및 성능 평가 — 성능 평가 시각화
5. 결과 해석 및 기대 효과
6. 한계점 및 향후 발전 방향
7. 팀 활동 소감 및 역할 기여도 (팀원별 담당 역할 포함)
8. 참고문헌

### 4. 분석코드
GitHub 링크 또는 Zip 파일 (7/30까지)

### 5. 발표자료
자유양식 (7/30까지)

## 확정된 프로젝트
- 주제: **약물 상호작용 확인 챗봇** (상세: `기획서_약물상호작용챗봇.md`)
- 참가 분야: 심화활용(Innovation, 기술기반) — 공공데이터(식약처 DUR·허가정보) 활용
- AI 모델: KoELECTRA 직접 파인튜닝(의도 분류 + NER) + KoGPT2 직접 파인튜닝(응답 생성, 사실 검증 게이트 + 템플릿 폴백). 외부 AI API 사용 안 함 — 전부 로컬 가중치.
- 학습 데이터: 허가정보 제품명 사전 × 질문 템플릿 슬롯 채우기로 직접 합성

## TODO
- [x] 작품 주제, 참가 분야 확정
- [x] 사전학습 모델 다운로드 → `models/koelectra-small-v3-discriminator` (55MB), `models/kogpt2-base-v2` (493MB). 로컬 경로 로딩 검증 완료. GitHub 업로드 시 models/는 .gitignore 처리(용량 초과 방지)
- [x] 공공데이터포털 API 인증키 발급 → `public data api/` 폴더에 엔드포인트·키 정리됨 (4개 API 공통 키 1개, GitHub 업로드 금지 대상)
- [ ] 팀명 확정, 팀원 정보 (성명/학과/학년/학번/연락처/이메일) 취합
- [ ] 제안서 초안 작성 (자유양식, ~7/17) — 기획서 기반
- [ ] 참가신청서 서명 이미지 준비
- [x] 데이터 수집 완료 → `data/drug.db` (2.3GB, 11개 테이블, 20만 행. `scripts/collect_data.py`로 재수집 가능). 킬러 데모 검증: 타이레놀·판콜 모두 M040353(아세트아미노펜) 함유 + DUR 효능군중복 규칙 존재 확인
- [x] 전처리 완료 → `data/drug_light.db` (43.4MB, `scripts/build_light_db.py`). M코드↔D코드 매핑 2,194건(복합 규칙 ORI 오염 제거), norm_name 정규화, DEL_YN 필터. 복합 규칙(mix_type='복합')은 엔진에서 "성분 조합 전체 포함" 조건으로 별도 처리 필요
- [x] 매칭 엔진 완료 → `src/dictionary.py`(사전+오토마타), `src/matcher.py`(추출+오타보정), `src/risk_engine.py`(검사 6종: 병용금기·성분중복(+용량주의 근거 보강)·효능군중복·노인주의·임부금기 등급별·특정연령대금기 나이 파싱). 테스트 `tests/test_engine.py` 12건 통과. 투여기간주의는 v1 제외(향후 발전)
- [x] 학습 데이터 합성 완료 → `scripts/synthesize_data.py`, `data/synth/` 4만 문장 (NER 오프셋 라벨 + 의도 4클래스, 오타 20% 주입, 조사 자동 처리)
- [x] KoELECTRA 학습 스크립트 완료 → `scripts/train_koelectra.py` (NER/의도 공용, 순수 PyTorch 루프, 스팬 F1 평가). CPU 스모크 테스트 통과
- [x] Colab 실학습 완료 → models/ner-drug, models/intent-drug 배치됨. `src/nlu.py` 래퍼로 파이프라인 통합(사전 매칭 → NER 보완 → 유사도, NER 스팬 어절 경계 확장 처리). 매칭 우선순위: 완전일치 > 접두일치(되묻기) > 자모 유사도. 테스트 16건 통과. ※ 보고서용 Colab test 지표(F1/acc) 캡처본 사용자 보유 여부 확인 필요
- 스코프 확인: 의약품 허가 영양제(아로나민·삐콤씨·레모나 등) 지원 O / 건강기능식품(고려은단·센트룸 등) 지원 X → 미인식 안내 응답 + 향후 발전
- [x] 챗봇 완성 (UI 방향 변경: Streamlit → FastAPI + 웹 채팅) → `src/api.py`(FastAPI), `src/responder.py`(템플릿 응답: 등급별 안내·되묻기·e약은요 부작용/복용법·건기식 미지원 안내), `web/`(채팅 UI: 노인/임산부 토글·나이 입력·되묻기 버튼·등급색 표시). 로컬 실행: `uvicorn api:app --app-dir src --port 8000`. 브라우저 검증 완료
- [x] 배포 준비 → `Dockerfile`(HF Spaces용), `requirements.txt`, `web/config.js`(API 주소), `deploy/DEPLOY_GUIDE.md`. 구조: Vercel(프론트) + HF Spaces(백엔드, Vercel은 PyTorch 불가). 실제 배포는 사용자 계정 필요
- [x] KoGPT2 응답 생성 파이프라인 완료 → `scripts/synthesize_gpt_data.py`(9,000쌍, 직렬화 형식이 엔진 출력과 동일), `scripts/train_kogpt2.py`(프롬프트 마스킹 학습, 토크나이저 특수토큰 명시 필수 — PreTrainedTokenizerFast), `src/generator.py`(생성 + 4중 검증 게이트: 숫자·약이름·성분·판정일관성), responder/api 연결(검증 실패 시 템플릿 폴백, 게이트 동작 검증 완료)
- [x] KoGPT2 실학습 완료 → models/kogpt2-drug(481MB) 배치. 생성은 greedy로 전환(샘플링은 이름 복사를 망가뜨림), 게이트에 인용 검증 추가(따옴표·괄호 안 문자열은 입력에 그대로 있어야 통과). 현재 품질: 정보없음 응답은 깨끗하게 통과, 금기·중복 응답은 이름 뭉개짐으로 폐기→템플릿 폴백(안전 동작 확인). ※ 통과율 올리려면 Colab에서 --epochs 5 재학습 검토. 게이트 통과율 자체가 보고서 성능 지표
- [x] HF Space 배포 완료 (kwangeun2/yaktalk-api) — KoGPT2 포함 전체 아키텍처 프로덕션 검증: 정보없음 응답은 KoGPT2 생성(4.9s), 금기·중복 응답은 게이트 폐기→템플릿 폴백(9.3s). https://kwangeun2-yaktalk-api.hf.space (web UI 포함 서빙). six 의존성·DB LFS·CRLF 이슈 해결 이력 있음
- [ ] Vercel 프론트 배포 (사용자 작업): web/config.js에 `window.API_BASE = "https://kwangeun2-yaktalk-api.hf.space"` 설정 후 web/ 폴더 배포
- [x] 실제 질문 100문장 테스트셋 + 성능 평가 완료 → `tests/testset_100.jsonl`, `scripts/evaluate.py`, 결과 `data/eval_report.md`. **추출 98% (F1 0.993, 정밀도 1.000) / 의도 95% / 판정 22/22 (100%)**. 이 과정에서 버그 6종 수정(성분명 사전+염 접미사 제거로 와파린 해결, 스캔 어절 경계, NER 불용어, 2글자 임계, 영문·숫자 정확 일치, 첫 글자 동점 우선) + 프로덕션 재배포 검증
- 남은 한계(보고서용): 건기식 브랜드가 유사 의약품명으로 되묻기됨(루테인→로테인 제안, 자동 확정은 안 됨), 의도 4건 오분류(약 2개면 병용 우선 규칙으로 판정 영향 없음), DUR 병용금기는 절대금기만 수록(와파린+아스피린 같은 '병용주의'급은 정보없음 처리 — 정직한 한계)
- [x] 2차 버그 헌트 완료: 크래시 13종 입력·판정 대칭 30쌍·무작위 500문장(100%) 통과. 발견·수정 3건 — 조사 오류('은(는)'→받침 판별), 카테고리 2개 되묻기 시 두 번째 항목 유실(clarify에 rest 필드 추가+UI 반영), 동일 규칙 다중 행의 근거 중복(dedupe). 브라우저 E2E(감기약+두통약→되묻기 2회→주의 판정) 검증, Space 재배포 완료
- [x] 성분 질문 응답 추가: "게보린 성분 알려줘"류가 부작용으로 오라우팅되던 문제. → `INGREDIENT_WORDS` 키워드 규칙 + `engine.ingredient_detail()`로 약 1개+성분질문 시 주성분·함량 목록 출력(`responder._ingredient_answer`). 라우팅은 `intent=='성분' OR 키워드` 이중 감지. 회귀 31건 통과, Space 재배포·프로덕션 검증. **재학습 준비 완료**: synthesize_data.py에 성분 템플릿 8종 추가→의도 5클래스(기타·병용·복용법·부작용·성분)로 data/synth 재생성됨(성분 3,987건), train_koelectra.py·nlu.py INTENT_LABELS 갱신, COLAB_GUIDE에 5클래스 의도 재학습 안내 추가. 실제 재학습은 사용자 Colab 작업(NER·KoGPT2는 무관하므로 의도만 재학습해 models/intent-drug 교체)
- [x] 3약물 되묻기 버그 수정: "게보린 타이레놀 판콜"처럼 확정 2개 + 접두일치 세 번째 약(판콜)일 때, 잡음 억제 규칙이 판콜까지 버려 2개만 판정하던 문제. → 카테고리·접두일치(score≥95)는 확정 개수와 무관하게 되묻기하고 저신뢰 유사도만 억제하도록 수정(responder). 되묻기→3개 전수비교 브라우저 E2E 검증, 회귀 27건 통과, Space 재배포. rest 필드는 웹 UI가 확정약을 drugs로 반영하므로 다른 되묻기 대상만 포함
- [x] 사용자 제출 실패 TC(TC.docx, 10건) 해결 완료. **엔티티 모델 도입**(제품 or 순수 D코드 성분): TC-03 3개+ 약물 전체 pairwise 비교(`check_multi`), TC-05 띄어쓰기 없는 입력 분해(scan에서 조사/접속사 gap 허용), TC-07 제품에 없는 순수 DUR 성분(플루복사민) D코드 직접 판정→병용금기 발동, TC-01 광범위 전문약 통칭(혈압약 등) 건기식과 구분된 안내, TC-04 술·음식 안내 문구, TC-09 성분중복에 약별 함량+고령자 용량 주의. 회귀 테스트 24건 통과, 100문장 지표 유지(98%/F1 0.993/판정 100%), Space 재배포·프로덕션 검증. 데이터 한계로 미해결: TC-06(심바스타틴+겜피브로질)·TC-08(리토나비르+리팜피신)은 한국 식약처 DUR 병용금기에 해당 쌍이 없음→임의 규칙 미생성(정직성 원칙), TC-06은 효능군중복으로 '주의' 노출. TC-00(와파린)은 기존 수정으로 이미 해결
